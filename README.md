# Model-Zoo
A collection of models trained by myself

## Getting started (Phase 1: Data Engineering)

If you're new to LLM fine-tuning, start here:

- `phase1_data/README.md` – download a small dataset, convert to SFT format, and get token-length stats.